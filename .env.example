# Base URL of the NIVARA classification API. When unset, the frontend
# runs in demo mode using a local rule-based classifier and seeded
# complaint history so the product remains fully demonstrable offline.
VITE_API_BASE_URL=
